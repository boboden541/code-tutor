# Файл №1 — Project Brief: <Название проекта>

> **Дата:** <YYYY-MM-DD>
> **Тип проекта:** `backend` | `frontend` | `fullstack`  ← обязательно
> **Статус:** Черновик (вход для `/tutor-requirements`)

## 1. Цель и краткое описание

<1–3 предложения: какую задачу решает продукт и для кого.>

## 2. Технологический стек

> **Стек опционален.** Если пользователь задал — переноси дословно. Если нет — оставь `[предложит /tutor-stack]` (НЕ выдумывай: обоснованно предложит шаг `/tutor-stack`).

| Категория | Технология |
|-----------|-----------|
| Backend: язык / фреймворк | <напр. Python / FastAPI> или `[предложит /tutor-stack]` |
| Backend: данные / ORM / миграции | <напр. PostgreSQL / SQLAlchemy / Alembic> или `[предложит /tutor-stack]` |
| Backend: аутентификация | <напр. JWT> или `[предложит /tutor-stack]` |
| Frontend: язык / фреймворк | <напр. TypeScript / React + Vite> или `[предложит /tutor-stack]` или `—` (если backend-only) |
| Frontend: состояние / роутинг | <напр. Pinia / Vue Router> или `[предложит /tutor-stack]` или `—` |
| Контейнеризация / CI/CD | <напр. Docker / GitLab CI> или `[предложит /tutor-stack]` |
| Хостинг / прод | <напр. VPS + Nginx + HTTPS> или `[предложит /tutor-stack]` |
| Прочее | <кэш, очереди, внешние API или `[предложит /tutor-stack]`> |

## 3. Ключевые сущности (домен)

| Сущность | Назначение | Ключевые поля (черновик) |
|----------|-----------|--------------------------|
| <напр. User> | <учётная запись> | <id, email, password_hash> |
| <напр. Note> | <заметка> | <id, title, body, owner_id> |

## 4. Акторы (роли пользователей)

| Актор | Кто это | Уровень доступа |
|-------|---------|-----------------|
| <напр. Гость> | <неавторизованный посетитель> | <только регистрация/вход> |
| <напр. Пользователь> | <авторизованный владелец данных> | <CRUD своих сущностей> |
| <напр. Администратор> | <управляет системой> | <полный доступ> или `[уточнить]` |

## 5. Пользовательские сценарии (use cases)

> Что и зачем делает каждый актор. Формат: «Как <актор>, я хочу <действие>, чтобы <цель>». Невыводимое — `[уточнить]`.

| ID | Актор | Сценарий (User Story) | Приоритет (MVP / потом) |
|----|-------|------------------------|--------------------------|
| UC-1 | <Гость> | Как гость, я хочу зарегистрироваться по email и паролю, чтобы получить доступ | MVP |
| UC-2 | <Гость> | Как гость, я хочу войти и получить токен, чтобы работать со своими данными | MVP |
| UC-3 | <Пользователь> | Как пользователь, я хочу создавать/читать/изменять/удалять <сущность>, чтобы <цель> | MVP |
| UC-4 | <Пользователь> | Как пользователь, я хочу искать/фильтровать <сущность>, чтобы быстро находить нужное | потом |
| UC-5 | <Администратор> | Как админ, я хочу <управляющее действие>, чтобы <цель> | `[уточнить]` |

**Основной happy-path (сквозной сценарий MVP):**
<напр.: регистрация → вход → создание заметки → получение списка заметок.>

**Краевые случаи и ошибки (черновик):**
- <напр. повторная регистрация с тем же email → 409>
- <напр. доступ к чужой сущности → 403>
- `[уточнить]`

## 6. Возможные требования к интерфейсам

> Точки взаимодействия продукта с внешним миром. Детализируются в Файле №2; здесь — черновик.

### 6.1 Тип интерфейса

- [ ] REST API (JSON) — основной
- [ ] Авто-документация OpenAPI/Swagger (`/docs`, `/redoc`)
- [ ] WebSocket / SSE (реалтайм) — `[уточнить]`
- [ ] Веб-UI (фронтенд) — `[уточнить]` (вне скоупа backend-книги, если не указано)
- [ ] CLI / админ-панель — `[уточнить]`

### 6.2 Черновик API-эндпоинтов

| Метод | Путь | Назначение | Актор |
|-------|------|-----------|-------|
| POST | `/api/v1/auth/register` | регистрация | Гость |
| POST | `/api/v1/auth/login` | вход, выдача токена | Гость |
| GET | `/api/v1/<сущность>` | список | Пользователь |
| POST | `/api/v1/<сущность>` | создание | Пользователь |
| GET/PUT/DELETE | `/api/v1/<сущность>/{id}` | чтение/изменение/удаление | Пользователь |

### 6.3 Соглашения интерфейса (черновик)

- Формат данных: JSON; версионирование пути: `/api/v1/...`.
- Аутентификация: <напр. `Authorization: Bearer <JWT>`> или `[уточнить]`.
- Формат ошибок: <напр. `{ "detail": "..." }`> или `[уточнить]`.
- Пагинация/сортировка списков: `[уточнить]`.

## 7. Границы MVP

**Входит в MVP:**
- <функция 1>
- <функция 2>

**Вне скоупа (потом):**
- <функция 3>

## 8. Известные ограничения и пожелания

- <напр. деплой на один VPS> / `[уточнить]`
- <напр. нагрузка до 100 RPS> / `[уточнить]`
- <напр. требования к безопасности: хэширование паролей, rate limiting> / `[уточнить]`

## 9. Профиль читателя и окружение

> Управляет глубиной книги: нужна ли глава 0, насколько подробны разборы, темп. Детализируется в Файле №2.

| Поле | Значение |
|------|----------|
| Уровень читателя | junior (только начал) / middle (уже писал API) |
| Операционная система | macOS / Linux (Ubuntu) / Windows |
| Что уже установлено | <напр. ничего / Python есть, Docker нет> или `[уточнить]` |
| Нужна ли глава 0 (установка окружения) | да (junior/чистая машина) / нет (middle) |

