# <Заголовок шага, человекочитаемый>

> **Глава → Секция:** <chapter_N_name> → <section_M_name>
> **Шаг N из M в секции.** Время: ~<10–30> минут.
> *(N = число из префикса файла `<KK>_…`; M = количество шагов в этой секции. Не выдумывать — брать из имени файла и плана.)*

## Что вы сделаете в этом шаге

<1–2 предложения: какой осязаемый результат появится в конце.>

## Предусловия

- Выполнен предыдущий шаг: [<имя>](../<путь>.md)
- Установлено: <напр. Python 3.12, Docker> (см. [setup_python_env.md](...))

## Зачем это нужно

<2–4 предложения: какую проблему решает шаг, как он вписан в чистую архитектуру / общий путь. Объясняем смысл, а не только действия.>

## Шаги

### 1. <Действие>

<Объяснение.> Создайте файл `app/<слой>/<file>.py` — **полный код файла** (без `...`, без сокращений; копируется целиком):

```python
# app/<слой>/<file>.py
# Полный, запускаемый код. Никаких многоточий и «(фрагмент)».
```

> **Правка существующего файла** (напр. `migrations/env.py`): показывать либо весь файл после правок, либо diff с якорями:
> «Найди строку `<X>` и замени на `<Y>`» / «После строки `<Z>` добавь `<…>`». Никогда — голый фрагмент без указания места.

#### Разбор кода

> Обязательный блок после КАЖДОГО нетривиального куска кода (см. принципы 1a/1b скила). Объясняем каждую новую конструкцию при первом появлении. Уже объяснённое ранее — кратко со ссылкой.

| Строка / конструкция | Что это | Что делает и зачем |
|----------------------|---------|--------------------|
| `<элемент 1>` | <термин/конструкция> | <простое объяснение + ментальная модель> |
| `<элемент 2>` | <...> | <...> |

**Новые термины:** **<термин>** — <определение одним предложением> (заносится в общий словарь, ср. `resources/glossary.md`).

> **Под капотом:** <если есть неявное поведение фреймворка — что и почему происходит автоматически (DI/автодоки/автогенерация миграций/маппинг ошибок)>.

> **Почему именно так:** <привязка к слою чистой архитектуры / требованию из Файла №2.>

### 2. <Следующее действие>

```bash
# команды оболочки с пояснением
...
```

## Проверка результата

Выполните и убедитесь в ожидаемом:

```bash
<команда проверки, напр. curl http://localhost:8000/health>
```

Ожидаемый результат:

```
<что должно вернуться, напр. {"status":"ok"}>
```

✅ Если видите это — шаг выполнен.

## Типичные ошибки

| Симптом | Причина | Решение |
|---------|---------|---------|
| <ошибка> | <причина> | <как починить> |

## Что дальше

Переходите к следующему шагу: [<имя следующего шага>](../<путь>.md)

---

> **Git-блок (для шага открытия фичи `start_feature_branch`):**
>
> ```bash
> git checkout main && git pull --ff-only
> git checkout -b feat/<feature-name>
> ```
>
> **Git-блок (для шага закрытия фичи `commit_push_open_mr`):**
>
> ```bash
> pytest -q                                   # тесты зелёные
> git add -A
> git commit -m "feat(<feature>): <описание>"
> git push -u origin feat/<feature-name>
> # открыть Merge Request в GitLab → дождаться зелёного CI → merge в main
> git checkout main && git pull --ff-only
> ```
>
> Обычные шаги реализации git-блок НЕ содержат — ветка уже активна. См. `resources/git_workflow.md`.
